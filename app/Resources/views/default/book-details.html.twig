{% extends 'base.html.twig' %}

{% block body %}

{% include 'default/header.html.twig' %}

    <div id="main">
    	<div class="container">
    		<div class="row">
    			<div class="col-md-6">
    				<div class="panel panel-default">
						  <ul>
                {% for b in books %}
						    <li>Tytuł: {{b.title}}</li>
                <li>Autor: {{b.author}}</li>
                <li>Opis: {{b.description}}</li>
                <li>Kategoria: {{b.category}}</li>
                <li>Wydawnictwo: {{b.publisher}}</li>
                {% endfor %}
						  </ul>
    				</div>
    			</div>
    		</div>
    	</div>
    </div>

{% endblock %}
