{% block body %}
<!--
<div class="col-md-3">
  <div class="panel panel-default">
    <ul>
      {% for category in categories %}
      <li><a href="/categories/{{category.id}}">{{category.title|e}}</a></li>
      {% endfor %}
    </ul>
  </div>
</div> -->

<div class="col-md-3">
    <div class="sidebar-nav">
        <div class="navbar navbar-default navbar-inverse" role="navigation">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".sidebar-navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                </button>
                <span class="visible-xs navbar-brand">KATEGORIE</span>
            </div>
            <div class="navbar-collapse collapse sidebar-navbar-collapse">
                <ul class="nav navbar-nav">  
                    <li class="sidebar-brand"><a href="#">KATEGORIE</a></li>          
                    {% for category in categories %}
                    <li class="active"><a href="/categories/{{category.id}}">{{category.title|e}}</a></li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </div>
</div>

{% endblock %}
